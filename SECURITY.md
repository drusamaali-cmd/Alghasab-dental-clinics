# 🔐 دليل الأمان - عيادات الغصاب

## بيانات الدخول الأولية

### لوحة التحكم (الإدارة)

```
الرابط: https://your-domain.com/admin/login
Username: admin
Password: Admin@Alghasab2025
```

⚠️ **مهم جداً:** غيّر كلمة المرور فوراً بعد أول تسجيل دخول!

## كيفية تغيير كلمة المرور

### من لوحة التحكم (موصى به)

1. سجّل الدخول إلى لوحة التحكم
2. اضغط على زر "تغيير كلمة المرور" في أعلى يمين الصفحة
3. أدخل:
   - كلمة المرور الحالية: `Admin@Alghasab2025`
   - كلمة المرور الجديدة (8 أحرف على الأقل)
   - تأكيد كلمة المرور الجديدة
4. اضغط "تغيير كلمة المرور"

### يدوياً من قاعدة البيانات

إذا نسيت كلمة المرور، يمكنك إنشاء حساب جديد:

```bash
python3 << 'EOF'
import asyncio
from motor.motor_asyncio import AsyncIOMotorClient
from passlib.context import CryptContext
import uuid
from datetime import datetime, timezone

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

async def reset_admin_password():
    client = AsyncIOMotorClient("mongodb://localhost:27017")
    db = client["alghasab_clinic"]
    
    # كلمة المرور الجديدة
    new_password = "YourNewSecurePassword@2025"
    
    # تحديث كلمة المرور
    result = await db.admin_users.update_one(
        {"username": "admin"},
        {"$set": {"password_hash": pwd_context.hash(new_password)}}
    )
    
    if result.modified_count > 0:
        print("✅ تم تغيير كلمة المرور بنجاح!")
        print(f"كلمة المرور الجديدة: {new_password}")
    else:
        print("❌ لم يتم العثور على المستخدم")
    
    client.close()

asyncio.run(reset_admin_password())
EOF
```

## معايير كلمة المرور القوية

✅ **يجب أن تحتوي على:**
- 8 أحرف على الأقل
- حروف كبيرة وصغيرة (A-Z, a-z)
- أرقام (0-9)
- رموز خاصة (@, #, $, %, &)

❌ **تجنب:**
- كلمات شائعة (admin, password, 123456)
- معلومات شخصية (تاريخ الميلاد، الاسم)
- تكرار الأحرف (aaa, 111)

### أمثلة لكلمات مرور قوية

```
Alghasab@Clinic2025
SecureDental#2025
Tooth$Care2025!
```

## الإجراءات الأمنية الموصى بها

### 1. تغيير كلمة المرور بانتظام

- غيّر كلمة المرور كل 90 يوم
- لا تستخدم كلمات مرور قديمة

### 2. لا تشارك بيانات الدخول

- احتفظ ببيانات الدخول في مكان آمن
- لا ترسلها عبر البريد الإلكتروني أو الرسائل
- استخدم مدير كلمات مرور (Password Manager)

### 3. تسجيل الخروج بعد الاستخدام

- دائماً سجّل الخروج بعد الانتهاء
- خاصة على الأجهزة المشتركة

### 4. استخدم HTTPS

- تأكد من استخدام HTTPS في جميع الأوقات
- لا تستخدم HTTP في الإنتاج

### 5. حدّث النظام بانتظام

- تحقق من التحديثات الأمنية
- قم بتحديث المكتبات والحزم

## حماية قاعدة البيانات

### 1. النسخ الاحتياطي

قم بعمل نسخة احتياطية يومية:

```bash
# نسخ احتياطي يدوي
mongodump --db alghasab_clinic --out /backup/$(date +%Y%m%d)

# نسخ احتياطي تلقائي (يومي في الساعة 2 صباحاً)
# أضف إلى crontab:
0 2 * * * mongodump --db alghasab_clinic --out /backup/$(date +\%Y\%m\%d)
```

### 2. تقييد الوصول

```bash
# في ملف MongoDB config
security:
  authorization: enabled
```

## استرجاع كلمة المرور

إذا نسيت كلمة المرور تماماً:

1. اتصل بالدعم الفني
2. أو استخدم السكريبت أعلاه لإعادة تعيين كلمة المرور

## التحقق من النشاط المشبوه

### علامات التحذير:

- محاولات تسجيل دخول فاشلة متعددة
- تغييرات غير معروفة في البيانات
- نشاط في أوقات غير عادية

### الإجراءات:

1. غيّر كلمة المرور فوراً
2. راجع سجلات النظام
3. اتصل بالدعم الفني

## جهات الاتصال

**الدعم الفني:**
- البريد الإلكتروني: security@alghasab-clinic.com
- الهاتف: 0501234567
- WhatsApp: 0501234567

---

**آخر تحديث:** نوفمبر 2025
